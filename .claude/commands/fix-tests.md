---
description: Fix failing tests by analyzing test reports and fixing all errors before re-running
---

Use the fix-tests skill to fix failing tests in the module: $ARGUMENTS (or all modules if not specified).

Follow the workflow defined in `.claude/skills/fix-tests/SKILL.md`.