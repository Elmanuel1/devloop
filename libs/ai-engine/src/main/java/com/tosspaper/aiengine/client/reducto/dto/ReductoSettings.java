package com.tosspaper.aiengine.client.reducto.dto;

import com.tosspaper.aiengine.client.common.dto.Settings;

/**
 * Settings for Reducto extract request.
 * Uses the generic Settings class directly.
 */
public class ReductoSettings extends Settings {
    // No additional fields needed - uses generic Settings directly
}
